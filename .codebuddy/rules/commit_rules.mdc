---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-08-29T15:36:28.985Z
provider: 
---

# Git 提交规范

## 一、核心流程

1. **提交前先更新工作记忆**：本轮工作全部完成后，先把结论、经验、教训写入 `.codebuddy/memory/`
   （当日日志与 MEMORY.md），再统一 `git add` 提交。确保一次 commit 同时包含
   「文档/代码改动 + 工作记忆」，避免提交后再改 memory 造成仓库不干净——
   这样换机器 `git pull` 下来即为完整状态。
2. **工作记忆中不记录 commit hash**：提交内容包含 memory 自身，把 hash 写进 memory 后内容即变、
   hash 随即失效（自指悖论，记录必然滞后一位）。定位靠「**日期 + 提交标题**」，
   需要时用 `git log --grep="<标题关键词>" --format="%h %s" -1` 查询。
   **不使用 git tag 做标记**：memory 已记录日期，tag 只是重复标记日期，属多余。
3. **工作记忆的记录要求**：**日期 + 详细步骤 + 与 commit message 对应的内容描述**。
   日志条目应能逐条对应本次提交 message 中的各项，便于日后对照某次提交具体做了什么。
4. 用户要求提交时，**先写好完整的 commit message 交给用户确认**，得到明确确认后才执行 `git commit`；**未确认不得提交**。
5. 每次提交**只允许生成一个 commit**，不允许自动拆成多个提交。
6. **默认只提交、不 Push**。Push 只在用户明确说出「push / 推送」时执行；
   「提交」「commit」不等于 push。**未获明确指示绝不 push，也不询问后抢跑**。

## 二、对外可见变更需用户确认（新增红线）

以下内容直接影响访客/远端仓库呈现，**改之前必须把具体内容给用户过目，确认后才能写入**：

1. **版本号**：`package.json` 的 `version`、changelog 条目的版本号（新增条目还是并入现有条目，由用户决定）。
2. **changelog / history 页面文案**：条目标题、日期、每条描述的措辞，需展示原文等确认。
3. **工具名称、描述等站点元信息**（`src/config/tools.ts`、`ToolLayout` 的 title/description）。
4. 其他「说出去就收不回」的内容：对外承诺（如「永久免费」）、隐私声明等。

反例（2026-08-30 实际发生）：擅自决定不更新版本号、擅自改写 changelog 条目标题与描述。
正确做法：先给出候选文案（如「方案 A：并入 1.6.0 / 方案 B：新增 1.6.1」），等用户选择。

## 三、message 编写要求

1. 支持中文，必须避免乱码。
2. **中文 message 必须写入 UTF-8 编码文件，再用 `git commit -F <file>` 提交**。
   - 原因：PowerShell 下直接用 `git commit -m` 传递多行中文，会因 GBK/UTF-8 编码不一致导致乱码，甚至因引号配对被破坏而命令解析失败。
   - 单行中文 `-m` 同样有乱码风险，统一用 `-F` 最稳妥。
3. 格式：第一行为标题（一句话概括本次改动），空一行后用分条正文说明改了什么、为什么。
4. 内容要求：客观描述改动，如实反映文档/代码/资源的增删改；不夸大、不遗漏重要项。

## 四、提交前检查清单

1. `git status --short` 确认改动范围。
2. 确认大文件与原始素材（PDF、高清图片等）已被 `.gitignore` 排除，未误入暂存区。
3. `git add -A` 后用 `git diff --cached --stat` 与 `--name-only` 核实暂存内容边界（文件数、行数、无无关文件混入）。
4. 检查是否有临时脚本、扫描报告等无关文件混入；`.codebuddy` 下仅 `memory/` 与 `skills/` 应入库。
5. 提交后复核：`git log -1` 确认 message 中文正常。

## 五、禁止的操作（安全红线）

1. 不执行 force push、hard reset、跳过钩子（`--no-verify`）。
2. **不改写已提交历史**（`git reset --soft/mixed HEAD~1`、`git commit --amend` 等），除非用户明确要求。
3. 不修改 git config（用户名、邮箱等）。
4. 不清理未跟踪文件（`git clean`）等破坏性操作。
